<nav class="nav">
        <div class="container">
            <div class="logo">
                <a href="#">Your Logo</a>
            </div>
      <% if current_user == nil %>
            <div id="mainListDiv" class="main_list">
                <ul class="navlinks">
                    <li><%= link_to "Se connecter", new_user_session_path%></li>
                    <li> <%= link_to "S'inscrire", new_user_registration_path%></li>
                </ul>
            </div>
      <% else %>
            <div id="mainListDiv" class="main_list">
                <ul class="navlinks">
                    <%= link_to "Mon profil", user_path(current_user)%>
                    <%= link_to "Se déconnecter", destroy_user_session_path, :method => 'DELETE' %>
                </ul>
            </div>
      <% end %>
            <span class="navTrigger">
               <i><li> <%= link_to "S'inscrire", new_user_registration_path%></li></i>

                <i></i>
                <i></i>
            </span>
        </div>



</nav>

